schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/requests-session-default-timeout"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh7188c2f8860ea56871ee98230ff1ae"
task_id = "requests-session-default-timeout"
display_title = "Add a default timeout to Requests sessions"
display_description = "Let a Session supply a default timeout while preserving per-request overrides and existing Requests behavior by default."
original_title = "为 Requests Session 增加默认请求超时"
category = "feature_request"
language = "python"
repository_url = "https://github.com/psf/requests"
base_commit_hash = "5460f467b02e49471c0fd6cfc9ca0adab6351f98"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary 5460f467b02e49471c0fd6cfc9ca0adab6351f98 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh7188c2f8860ea56871ee98230ff1ae-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
